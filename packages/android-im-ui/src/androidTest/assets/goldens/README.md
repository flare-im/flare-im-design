# Compose baselines

Generate `message-status-rc.png` only on the pinned API 35 / 420 dpi runner
declared in `tests/visual/compose/runner.json`, review it, then run the
compare command. A baseline from an arbitrary emulator must not be committed.

<!-- GENERATED by website/scripts/build-library-guides.mjs. -->

# Recipes

These compositions use only public components from the Component Catalog.

## Build a Complete Mobile IM App

MobileAppShell + AdaptiveNavigation + ConversationWorkspace + WorkspaceFrame (contacts / settings scenes); safe areas and the soft keyboard participate in layout.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build a Complete Desktop IM App

DesktopAppShell + AdaptiveNavigation + ConversationWorkspace + WorkspaceFrame (contacts / search / settings scenes).

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Conversation List

ConversationListContainer + ConversationList + ConversationRow + Avatar; the host owns selection, paging, and unread projection.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Contacts

FriendListContainer + ContactList + ContactDetail + NewFriendRequests; the host owns relationship state.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Group Chat

WorkspaceFrame + GroupList + GroupDetail + GroupMemberGrid + ConversationWorkspace.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Message Timeline

MessageList + MessageBubble + MessageContentView + MessageMeta; preserve scroll anchors and unread boundaries.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Composer

Composer + ComposerReplyStrip + ComposerActionPanel + TransferQueue; draft and send state are controlled.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Search

SearchBar + SearchPanel + SearchResults; query, filter, and time range are controlled.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build File / Media Browser

WorkspaceFrame + MediaCenter + TransferQueue + MarkdownPreview + VideoPlayerModal.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Image Preview

ImagePreviewModal receives a resolved source, loading/error state, and a host save intent.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Calls UI

WorkspaceFrame + IncomingCall + CallView + GroupCallView + CallDevicePicker; the host provides RTC.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Offline Retry

StatusBanner + WorkspaceState + MessageStatus + retry intent; failed messages retain their client id.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Read Receipts

MessageLifecycle → visualStatus → MessageStatus / MessageMeta / ReadReceiptSheet.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Message Actions

MessageActionSheet + capability filtering + custom action extensions; the host performs side effects.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

## Build Dark Theme IM

Override semantic tokens and validate bubbles, read color, focus, and elevation without editing generated files.

Cover loading, empty, error/offline, and recovery; use the matching [component contracts](/en/components/).

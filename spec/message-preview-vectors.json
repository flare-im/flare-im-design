{
  "note": "Shared truth for a message's one-line summary: what a conversation row, a reply strip and a bubble's quote say about a message whose content is not plain text. Vue implements it in utils/messagePreview.ts (previewTextFromMessageContent); the three native kits implement the same rule over their own content models. `kind` is the content type; `fields` are the payload values a case needs, with these conventions: `text` sits at the content root, everything else is the type's own payload (image.description, file.fileName, location.title, linkCard.title, task.title, quote.quotedTextPreview); `count` means that many items in the list the type carries (forward.items, imageGroup.images); `animated` marks a motion image. `expected` is the summary for each locale. `replyExpected` is what a reply strip or a quote shows when the summary is empty — the summary itself stays empty so a conversation row can show nothing, while a reply never shows a blank line. Every platform's test reads this file; the words come from each kit's strings table (`preview.*`), never from an app. An album with no images says only what it is (`preview.imageGroup`), never \"0 of them\"; a forward says how many only when it carries more than one. An `emoji` case's `key` is a pack key, and its expectation is that key's **name in the reader's language** (`emoji-locales.json`, the same file in all four kits): a conversation row reads 一百分 rather than `hundred_points`. An unknown key falls back to the key itself. A `rich_text` case carries the one line the document reads as; a kit with no rich-text body receives it as `FlareGenericContent` under that wire type, like `quote`.",
  "fallbackKey": "preview.message",
  "cases": [
    { "id": "text", "kind": "text", "fields": { "text": "明天见" }, "expected": { "zh-CN": "明天见", "en-US": "明天见" } },
    { "id": "text-with-pack-token", "kind": "text", "fields": { "text": "晚上见 [hundred_points]" }, "expected": { "zh-CN": "晚上见 一百分", "en-US": "晚上见 hundred points" } },
    { "id": "text-blank", "kind": "text", "fields": { "text": "   " }, "expected": { "zh-CN": "", "en-US": "" }, "replyExpected": { "zh-CN": "[消息]", "en-US": "[Message]" } },
    { "id": "image", "kind": "image", "fields": {}, "expected": { "zh-CN": "[图片]", "en-US": "[Image]" } },
    { "id": "image-described", "kind": "image", "fields": { "description": "发布会封面" }, "expected": { "zh-CN": "发布会封面", "en-US": "发布会封面" } },
    { "id": "image-animated", "kind": "image", "fields": { "animated": true }, "expected": { "zh-CN": "[动图]", "en-US": "[GIF]" } },
    { "id": "video", "kind": "video", "fields": {}, "expected": { "zh-CN": "[视频]", "en-US": "[Video]" } },
    { "id": "audio", "kind": "audio", "fields": {}, "expected": { "zh-CN": "[语音]", "en-US": "[Voice]" } },
    { "id": "file", "kind": "file", "fields": {}, "expected": { "zh-CN": "[文件]", "en-US": "[File]" } },
    { "id": "file-named", "kind": "file", "fields": { "fileName": "报价单.pdf" }, "expected": { "zh-CN": "[文件] 报价单.pdf", "en-US": "[File] 报价单.pdf" } },
    { "id": "location", "kind": "location", "fields": {}, "expected": { "zh-CN": "[位置]", "en-US": "[Location]" } },
    { "id": "location-named", "kind": "location", "fields": { "title": "公司前台" }, "expected": { "zh-CN": "[位置] 公司前台", "en-US": "[Location] 公司前台" } },
    { "id": "card", "kind": "card", "fields": {}, "expected": { "zh-CN": "[名片]", "en-US": "[Contact]" } },
    { "id": "sticker", "kind": "sticker", "fields": {}, "expected": { "zh-CN": "[贴纸]", "en-US": "[Sticker]" } },
    { "id": "link-card", "kind": "link_card", "fields": {}, "expected": { "zh-CN": "[链接]", "en-US": "[Link]" } },
    { "id": "link-card-titled", "kind": "link_card", "fields": { "title": "2.0 发布说明" }, "expected": { "zh-CN": "2.0 发布说明", "en-US": "2.0 发布说明" } },
    { "id": "forward-many", "kind": "forward", "fields": { "count": 3 }, "expected": { "zh-CN": "[转发] 3 条消息", "en-US": "[Forward] 3 messages" } },
    { "id": "image-group", "kind": "image_group", "fields": { "count": 4 }, "expected": { "zh-CN": "[多图] 4 张", "en-US": "[Album] 4" } },
    { "id": "image-group-empty", "kind": "image_group", "fields": { "count": 0 }, "expected": { "zh-CN": "[多图]", "en-US": "[Album]" } },
    { "id": "vote", "kind": "vote", "fields": {}, "expected": { "zh-CN": "[投票]", "en-US": "[Poll]" } },
    { "id": "task-titled", "kind": "task", "fields": { "title": "周报" }, "expected": { "zh-CN": "周报", "en-US": "周报" } },
    { "id": "announcement", "kind": "announcement", "fields": {}, "expected": { "zh-CN": "[公告]", "en-US": "[Announcement]" } },
    { "id": "quote-of-text", "kind": "quote", "fields": { "quotedTextPreview": "上一条消息" }, "expected": { "zh-CN": "上一条消息", "en-US": "上一条消息" } },
    { "id": "system", "kind": "system", "fields": {}, "expected": { "zh-CN": "[系统消息]", "en-US": "[System]" } },
    { "id": "emoji-pack-key", "kind": "emoji", "fields": { "key": "hundred_points" }, "expected": { "zh-CN": "一百分", "en-US": "hundred points" } },
    { "id": "emoji-unknown-key", "kind": "emoji", "fields": { "key": "not_a_pack_key" }, "expected": { "zh-CN": "not_a_pack_key", "en-US": "not_a_pack_key" } },
    { "id": "emoji-no-key", "kind": "emoji", "fields": {}, "expected": { "zh-CN": "[表情]", "en-US": "[Emoji]" } },
    { "id": "rich-text", "kind": "rich_text", "fields": { "plainText": "周报已发出" }, "expected": { "zh-CN": "周报已发出", "en-US": "周报已发出" } },
    { "id": "rich-text-empty", "kind": "rich_text", "fields": {}, "expected": { "zh-CN": "[富文本]", "en-US": "[Rich text]" } },
    { "id": "mini-program-titled", "kind": "mini_program", "fields": { "title": "会议室预订" }, "expected": { "zh-CN": "会议室预订", "en-US": "会议室预订" } },
    { "id": "mini-program", "kind": "mini_program", "fields": {}, "expected": { "zh-CN": "[小程序]", "en-US": "[Mini Program]" } }
  ]
}

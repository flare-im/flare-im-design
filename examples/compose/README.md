# Compose example

This Android app consumes `packages/android-im-ui` as a Gradle project and
demonstrates the six brand themes, light/dark mode, conversation selection,
incoming/outgoing messages, read status, and a primary action.

Build from the repository root:

```bash
packages/android-im-ui/gradlew -p examples/compose :app:assembleDebug
```

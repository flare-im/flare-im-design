name: flare_im_ui_example
description: Gallery app showcasing every flare_im_ui component with mock data.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.1
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flare_im_ui:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

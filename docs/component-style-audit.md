# Stable Public Component Style Audit

Generated from spec/components.json and spec/component-catalog.json. It covers every Stable Public Component and records the post-fix state. P0/P1 findings are either fixed or guarded by an existing registry/capability contract; remaining work is limited to demonstrated P2/P3 drift.

## Audit method

The audit checks typography, spacing, padding, alignment, icon scale and stroke, radius, border, elevation, surfaces, semantic color, hierarchy, density, hover, focus, pressed, selected, disabled, loading, error, empty, responsive behavior, accessibility, and motion. Automated gates cover raw values, signatures, tokens, themes, public exports, documentation, and interaction tests. Native visual regression remains in each platform test project.

## Cross-cutting findings

- Button, Input, Menu, Navigation, and ConversationItem use the shared control and density scales; local height changes require a named token.
- Icon sizing is limited to semantic sm/md/lg scales. The AdaptiveNavigation 22px exception was removed.
- Radius follows xs/sm/md/lg/xl/full roles. Normal rows and page sections remain flat.
- Selected, hover, focus, disabled, loading, error, and empty states consume semantic tokens and retain stable dimensions.
- Violet, Ocean, Forest, Sunset, Rose, and Graphite remain palette identities owned by theme mapping, not public components.
- The documentation preview is no longer a platform/theme/density/state test dashboard.

## Component matrix

| Component | Category | Current Design | Inconsistency | Interaction Problem | Configurability Problem | Recommended Change | Priority |
|---|---|---|---|---|---|---|---|
| Avatar | General | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| AppLayout | Layout | Responsive structural surface; semantic tokens; responsive, slots/builders | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| AdaptiveNavigation | Navigation | General UI primitive; semantic tokens; responsive | Resolved: selected, hover, focus, disabled, badge, and semantic icon sizing use shared tokens. | Desktop keyboard movement and mobile bottom navigation preserve the same IDs. | Default IM and contact navigation can be fully replaced, hidden, gated, reordered, and extended. | Resolve navigation once in the host and reuse it across bottom, rail, and sidebar presentations. | P1 fixed |
| MobileAppShell | Layout | Responsive structural surface; semantic tokens; responsive, slots/builders | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| DesktopAppShell | Layout | Responsive structural surface; semantic tokens; responsive, slots/builders | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| ConversationListContainer | Patterns | Responsive structural surface; semantic tokens; responsive, slots/builders | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| FriendListContainer | Patterns | Responsive structural surface; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| WorkspaceFrame | Workspaces | Responsive structural surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| IMAppKit | AppKit | Responsive structural surface; semantic tokens; responsive, capabilities, slots/builders | Application shell, workspaces, capabilities, and navigation use shared semantic contracts. | Presentation changes by responsive mode without changing navigation intent. | Targeted region overrides compose advanced products without builder-per-pixel APIs. | Keep simple presets small and move product routing into the host adapter. | P1 guarded |
| MessageStatus | Message | IM semantic surface; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| MessageMeta | Message | IM semantic surface; semantic tokens; density | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| ConversationList | Conversation | IM semantic surface; semantic tokens; responsive, capabilities, slots/builders | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| ConversationRow | Conversation | IM semantic surface; semantic tokens; responsive, capabilities | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| ConversationDetails | Conversation | IM semantic surface; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| StartConversationDialog | Conversation | IM semantic surface; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MessageBubble | Message | IM semantic surface; semantic tokens; responsive, slots/builders | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| MessageList | Message | IM semantic surface; semantic tokens; responsive, slots/builders | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| ConversationHeader | Message | IM semantic surface; semantic tokens; responsive, capabilities, slots/builders | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| ChatWorkspace | Layout | Responsive structural surface; semantic tokens; responsive, slots/builders | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| PinnedMessageBar | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MessageContentView | Message | IM semantic surface; semantic tokens; slots/builders | Built-in message types already dispatch through a renderer registry on all platforms. | Unknown content keeps a visible fallback instead of failing silently. | Host renderers support business types without modifying MessageBubble. | Add business renderers through registry tests, never through a growing switch. | P1 guarded |
| TextMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| RichTextMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ImageMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ImageGroupMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| VideoMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| VoiceMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| FileMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| LocationMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ContactMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| LinkCardMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| VoteMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| TaskMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| StickerMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| EmojiMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| SystemMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| Composer | Composer | IM semantic surface; semantic tokens; responsive, capabilities, slots/builders | Resolved: legacy attachActions/op naming and over-broad defaults diverged from native semantics. | Resolved: uncontrolled Plus state now opens reliably; PC and mobile consume one action list. | Resolved: defaults, capability filtering, full replacement, hide, disable, reorder, custom icon/label/intent. | Keep the five-action default and validate targeted slots/builders without adding boolean flags. | P0 fixed |
| VoiceHoldButton | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ComposerActionPanel | Composer | IM semantic surface; semantic tokens; responsive, capabilities | Resolved: all platforms now consume the canonical composer action contract. | Disabled actions remain visible and expose their reason. | Action availability is host-owned through capabilities. | Keep desktop and mobile presentation separate from action semantics. | P1 fixed |
| EmojiStickerPicker | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ComposerMediaPreview | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ComposerSendButton | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ComposerReplyStrip | Composer | IM semantic surface; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| RichMarkdownInput | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MessageActionSheet | Message | IM semantic surface; semantic tokens; responsive, capabilities | Resolved: attachment sheet defaults and action fields now match Composer on all platforms. | Disabled actions remain discoverable; hidden and unavailable actions do not leave empty gaps. | Host lists can remove, hide, disable, reorder, relabel, re-icon, and add business actions. | Keep message command capabilities in InteractionState and host extensions; do not merge them with attachment IDs. | P1 fixed |
| ImagePreviewModal | Media | General UI primitive; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| VideoPlayerModal | Media | General UI primitive; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MarkdownPreview | Media | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| SearchBar | Navigation | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| Input | Form | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| InviteCodeField | Form | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| EmptyState | Feedback | General UI primitive; semantic tokens; slots/builders | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| StatusBanner | Feedback | General UI primitive; semantic tokens; slots/builders | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| FilterTabs | Navigation | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| ContactList | Contacts | IM semantic surface; semantic tokens; responsive, slots/builders | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ContactItem | Contacts | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ContactDetail | Contacts | IM semantic surface; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| GroupDetail | Contacts | IM semantic surface; semantic tokens; responsive, capabilities | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| NewFriendRequests | Contacts | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| GroupList | Contacts | IM semantic surface; semantic tokens; responsive, slots/builders | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ProfilePanel | Profile | General UI primitive; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ProfileEditor | Profile | General UI primitive; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| SettingsList | Profile | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| CallView | Call | General UI primitive; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| IncomingCall | Call | General UI primitive; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| CallControls | Call | General UI primitive; semantic tokens; capabilities | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ResponsiveLayout | Layout | Responsive structural surface; semantic tokens; responsive, slots/builders | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| GroupCallView | Call | General UI primitive; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| TypingIndicator | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| UnreadDivider | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ScrollToLatest | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ProfileCard | Profile | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| GroupMemberGrid | Contacts | IM semantic surface; semantic tokens; responsive, capabilities | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ReactionSummary | Message | IM semantic surface; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| ReadReceiptSheet | Message | IM semantic surface; semantic tokens; responsive | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| MessageBatchToolbar | Message | IM semantic surface; semantic tokens; capabilities | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MentionPicker | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| QuickPhrases | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| SearchResults | Feedback | General UI primitive; semantic tokens; slots/builders | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| Skeleton | Feedback | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| ForwardPicker | Conversation | IM semantic surface; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| Toast | Feedback | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| CallDock | Call | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| AnnouncementBanner | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| DatePill | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| RedPacketCard | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| SlashCommandMenu | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| TranslationView | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| QRCard | Profile | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MyInvitePanel | Profile | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ImageGrid | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| VoiceRecordingBar | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| PollComposer | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ChatWallpaperPicker | Conversation | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ConversationActionSheet | Conversation | IM semantic surface; semantic tokens; responsive, capabilities | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ConversationBatchToolbar | Conversation | IM semantic surface; semantic tokens; capabilities | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| VoicePlayer | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| UnknownMessage | Message | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| EmojiPicker | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| StickerPanel | Composer | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| SegmentedControl | Navigation | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ScreenHeader | Layout | Responsive structural surface; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MomentCard | Moments | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MomentComposer | Moments | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MomentActionPopover | Moments | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MomentsCoverHeader | Moments | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| CommentThread | Moments | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| TopicChip | Moments | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| Button | General | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| IconButton | General | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| FormField | Form | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| Switch | Form | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| Checkbox | Form | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| RadioGroup | Form | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| Select | Form | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| Textarea | Form | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| Stepper | Form | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| Slider | Form | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| Rating | Form | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| TimePicker | Form | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| DatePicker | Form | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| Icon | General | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ConfigProvider | General | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ConversationWorkspace | Layout | Responsive structural surface; semantic tokens; responsive, slots/builders | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MomentsVisibilityRuleList | Moments | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ContactMatchList | Contacts | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| AnnouncementReadBar | Feedback | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MomentAudienceSheet | Moments | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| TransferProgress | Media | General UI primitive; semantic tokens; base controlled contract | No P0/P1 drift after token, signature, state, and platform contract gates. | Core states are explicit; platform-native input remains the presentation layer. | Public props/events are host-owned and SDK-neutral. | Keep under visual regression and raw-value drift gates; address only demonstrated P2 drift. | P2 guarded |
| SearchPanel | Overlay | Elevated overlay; semantic tokens; responsive, slots/builders | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| RecentSearches | Navigation | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| TransferQueue | Media | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| CapabilityBoundary | Feedback | General UI primitive; semantic tokens; capabilities | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| DangerConfirm | Overlay | Elevated overlay; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| PermissionPrompt | Feedback | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| SearchDateRangeFilter | Form | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MemberPanel | Contacts | IM semantic surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| UnknownUserPlaceholder | Contacts | IM semantic surface; semantic tokens; density | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| RelationActionBar | Contacts | IM semantic surface; semantic tokens; capabilities | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| GroupPermissionMatrix | Contacts | IM semantic surface; semantic tokens; capabilities | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MemberRoleSheet | Contacts | IM semantic surface; semantic tokens; capabilities | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| DeviceSessions | Profile | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| NotificationPreferences | Profile | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| StorageUsage | Profile | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| MediaCenter | Media | General UI primitive; semantic tokens; responsive | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| CallDevicePicker | Call | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ScreenShare | Call | General UI primitive; semantic tokens; capabilities | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| CommandPalette | Navigation | General UI primitive; semantic tokens; responsive, slots/builders | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| Screen | Layout | Responsive structural surface; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| BottomSheet | Overlay | Elevated overlay; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| ActionMenu | Overlay | Elevated overlay; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| FormSheet | Overlay | Elevated overlay; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| SettingsRow | Profile | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |
| BrandLogo | General | General UI primitive; semantic tokens; base controlled contract | No blocking drift found; inherits the category token and state grammar. | Uses the shared focus, touch-target, disabled, and recovery expectations. | Configuration is limited to its public task boundary. | Retain current boundary and review with its category when behavior changes. | P3 monitor |

## Priority summary

- P0 fixed: Composer canonical actions and default interaction.
- P1 fixed or guarded: ComposerActionPanel, MessageActionSheet, AdaptiveNavigation, renderer registry, and AppKit composition.
- P2 guarded: core visual components remain under token, state, and visual regression gates.
- P3 monitor: focused primitives retain their existing boundary unless a real product inconsistency appears.

Audited components: 154.

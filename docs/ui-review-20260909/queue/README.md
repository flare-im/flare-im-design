# TransferQueue 验收 · 2026-09-09

- Web：191 项单元测试、174 SFC 编译、类型检查通过。
- Flutter：新增 2 项测试通过，验证批量能力筛选、320 宽/200% 字号、刷新失败保留任务及逐项恢复；修改文件 analyze 通过。
- Swift：27 项测试、iOS 16 模拟器目标编译通过。
- Android：示例 Kotlin 编译、组件单元测试通过（新增批量候选筛选回归）。
- 114 组件契约与四端符号检查、14 领域覆盖矩阵、文档站构建通过。文档构建仍有已有的大包体提示。
- 浏览器 320/390/1280 × 深浅主题：批量仅提交 a/b 两个失败任务，处理中禁用，部分失败后只剩 b 可批量重试，取消项保持取消；队列刷新错误保留任务，空队列正常，按钮至少 48px，无横向溢出。数据见 checks.json。

复现（在 flare-im-design 下）：

```sh
node scripts/check-transfer-queue-browser.mjs http://127.0.0.1:5189 ../flare-im-core-client-sdk/examples/flare-core-web-app/package.json
```

截图隐藏文档站固定导航，仅用于观察真实队列组件。演示为本地数据，不代表真实 SDK 传输或服务器验收。原生真机读屏、后台传输和账号切换集成尚未验收。本批没有部署服务器、发布组件包或接入示例应用任务源。

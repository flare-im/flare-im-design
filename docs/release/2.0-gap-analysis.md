# 2.0 Gap Analysis（2026-09-12，候选 2.0.0-rc.1）

目的：把「2.0.0 正式版的 Definition of Done」逐项对到当前证据，列出缺口、根因、优先级和验证方式。这是一份闭合台账，不是宣言：每一行的 Status 只写能被现有产物证明的状态。

证据来源：
- docs/2.0-readiness-matrix.md（上一轮 uekTHT 全量 36/36 检查）、docs/conversation-list-2.0-finalization.md
- 本轮审计：docs/release/component-architecture-audit.md、component-usage.md、component-duplication.md（数据 plans/audit-2026-09-12/inventory.json）
- 同日设计审计：plans/audit-2026-09-12/REPORT.md + findings-{message,composer-media-call,conversation-contacts-profile-moments,form-general-feedback-overlay-navigation,layout-workspaces-patterns-appkit}.md
- 同日已落地修复：plans/audit-2026-09-12/patch-waveA1-vue.patch（Vue 20 文件）、patch-waveA2-native.patch（原生 P0 + 契约）、patch-waveA3-applayout.patch（四端宽度感知 + 共享向量）、patch-waveA4-parity.patch（四端漂移）；验证：Vue vitest 410 / vue-tsc / 204 SFC 编译 / Playwright 66+，Flutter analyze 0 + 441 测试，Swift 183 测试，Compose 146 单测，spec validate + 8 个 tooling 门禁

Status 取值：PASS（有可复跑证据）· PARTIAL（部分证据）· FAIL（有证据证明不满足）· NONE（没有任何机制或证据）· MANUAL（只能真机）

## 1. 执行摘要

| 维度 | 现状 | 结论 |
|---|---|---|
| 组件数 | catalog 154 + Vue 未登记公开导出 29 | 公开面过大：113 KEEP / 28 MERGE / 15 REFACTOR / 11 INTERNALIZE / 10 DEPRECATE / 5 REMOVE / 1 SPLIT（architecture audit） |
| 真实消费 | 154 个里 116 个五个 example 都不用，40 个 example 与 kit 内部都不用 | Example 只是薄壳，不能证明 kit 可用；2.0 前每个 KEEP 组件必须有消费者或标 BETA |
| 重复 | 17 组语义重复（D1–D17），最大的是两套消息体（15 vs 20+5） | 必须合并，否则 API 冻结冻的是重复面 |
| 测试 | 58 个组件任何端无测试引用；132 个无 Playwright 引用 | 测试金字塔缺中层（component/contract），全靠 6 个 spec 的浏览器测试 |
| 平台契约 | 只有 Vue 的 ConfigProvider；无 PlatformAdapter / Capabilities / Native Error Model / native contract tests | 简报第 21–24、55 条整体为 NONE，是最大的架构缺口 |
| 可达性 | 契约元数据齐（134 个），但无 axe/契约级自动化；本轮修了 7 个 P0 中的 5 个 a11y/交互类 | Critical/Serious=0 目前不可证明 |
| 文档 | docs/release 只有 README；无 public-api / migration / catalog / maturity | 全部待生成 |

上表是 2026-09-12 审计当天的读数，保留原样作为起点。收口后的读数见 docs/release/2.0-final-report.md（结论 NOT READY → rc.2）与本文件第 2 节、3.1 节。

## 2. DoD 逐项对照

| # | DoD | Status | 证据 / 缺口 | 优先级 | 验证方式（2.0 门禁） |
|---|---|---|---|---|---|
| 1 | 所有 P0 PASS | FAIL | 第 3 节 P0-1…P0-13 全部落地并留证（见 3.1 与各行「DONE」）；仍为 FAIL 的原因不是代码：`spec/manual-evidence.json` 还有 13 条未销（9 条 AUTOMATION_REQUIRED 的真机 / 模拟器项 + 4 条 MANUAL_RELEASE_GATE），CI 的 Linux 视觉基线未生成，share / onNativeBack 与媒体 URL 收口仍是功能缺口 | P0 | release:check 全绿 + manual-evidence 清零 |
| 2 | 所有 public component 有明确 Purpose | PARTIAL | catalog summary 齐全，但 29 个 Vue 导出不在 catalog；7 个 Workspace 别名无独立目的（2026-09-12：未登记导出 29→7（pendingMerge），其余已登记 / 内部化 / 迁出）| P0 | check:public-exports 要求导出 ⊆ catalog |
| 3 | 无 Dead Public Component | PARTIAL | 2026-09-12：`check:dead-components` 上线（Vue 入口可达性 / 原生公开组件 catalog-or-consumer / 站点 demo 有主）；两波共删除或私有化 Flutter 5、iOS 5、Compose 4 个零消费者符号与 8 个孤儿 demo；2026-09-13 只余 Flutter `ComposerInlineTextField` 挂在 D16（P0-5） | P0 | check:dead-components --strict = 0 |
| 4 | 无 Duplicate IM Component | PARTIAL | 2026-09-12/13：`spec/duplication-register.json` + `check:duplicate-components` 上线；D3/D4/D5/D6/D7/D9/D10/D11/D12/D13/D14/N1 落地（catalog 159→149，pendingMerge 7→0）；余 D15/D16/D17/N2 归 P0-5/P0-8 | P0 | check:duplicate-components --strict = 0 open |
| 5 | 无无意义碎片 | PARTIAL | TimeStamp 已删、MasterDetail/ThreePane 已内部化（2026-09-12）；余 Composer 三子件（P0-5）、5 个 SceneList 包装（P0-8） | P1 | architecture audit 动作落地 |
| 6 | 无职责失控巨型组件 | PARTIAL | Composer 已拆（2026-09-13：编排器 878 行，职责边界写进契约 notes，部件与组合式各有单测）；MessageList 888 / MessageBubble 677 归 P1（duplication §5） | P0（Composer）/P1 | 拆分后行数不是指标，职责边界写进契约 + 单测 |
| 7 | Public/Internal 边界明确 | PARTIAL | PUBLIC_API.md 说 auth/diagnostics 不是包 API，components/index.ts 却导出 FlareAuthScreen/DiagnosticsConsole/WorkbenchShell 且 example 在用（2026-09-12：api-check 门禁 + 产品代码迁出；7 项待 P0-4 合并后转 DONE）| P0 | check:public-exports 对照 catalog；example 改用 examples/shared |
| 8 | Component maturity 明确 | NONE | 无 docs/components/component-maturity.md；catalog status 全是 "stable"（154/154），与 116 个零消费者矛盾 | P0 | 生成 maturity 表；零消费者不得 STABLE |
| 9 | Action Model 统一 | PARTIAL | ConversationActionSheet/MessageContextMenuSheet/MemberRoleSheet 消费同一 action 数据；附件动作却分裂在 MessageActionSheet(九宫格)/ComposerActionPanel（D8） | P1 | 一份 FlareAction 类型（id/label/icon/danger/disabled/visible/shortcut/onSelect）四处消费 |
| 10 | Message State Model 统一 | PARTIAL | 有 message-lifecycle 契约与 7 态；但已送达/已读只差颜色（F-02）、Vue MessageStatus prop 名与三端不同（F-09）、sending/retrying 同图（F-23） | P1 | 契约 props 四端交叉校验 + 7 态截图 |
| 11 | Platform Capability 完整 | PARTIAL | 无 capabilities 模型；平台判断散落（isH5、pointer: coarse、responsiveMode…）。ConfigProvider 仅 Vue（spec/validate：compose/ios 未实现）（2026-09-12：四端 PlatformCapabilities + 提供者已落地，组件消费从 useMessageMenuInteraction 起步）| P0 | PlatformProvider/usePlatformCapabilities 四端 + 契约测试 |
| 12 | Platform fallback 明确 | DONE | 无 docs/release/platform-fallback-policy.md（2026-09-12：docs/release/platform-fallback-policy.md）| P0 | 每个能力 SUPPORTED/FALLBACK/UNSUPPORTED |
| 13 | PlatformAdapter 完整 | DONE | 无 pickFile/pickImage/…/nativeBack 统一接口；能力散在 Composer 与宿主（2026-09-12：接口四端 + web/tauri/flutter 宿主实现；2026-09-13：iOS `IosPlatformAdapter` / Android `AndroidPlatformAdapter` 落地，五个宿主全部经适配器）| P0 | 接口 + 五端实现 + contract tests |
| 14 | Native Error Model 完整 | DONE | 无 UNSUPPORTED/CANCELLED/PERMISSION_DENIED/TIMEOUT/FAILED 语义（2026-09-12：五码 + 四端归一化测试）| P0 | 随 13 |
| 15 | Native Contract PASS | PARTIAL | 无 native contract tests（2026-09-12：契约测试四端 PASS_TEST；2026-09-13：五宿主适配器 + 宿主侧单测；真机 / 模拟器选择器往返仍未覆盖）| P0 | 随 13 |
| 16 | Examples parity | PARTIAL | readiness：Web 真实登录/双向消息 PASS；其它端只有构建/单测；无 example-parity 矩阵 | P0 | docs/release/example-parity.md（PASS_RUNTIME/PASS_TEST/PASS_BUILD/SIMULATED/UNSUPPORTED/FAIL） |
| 17 | Examples 不复制 Design System | DONE | examples/shared/vue-reference 有 5 个文件直接 import naive-ui / 用 `<n-*>`（2026-09-13：两个示例根 177 个源文件全部只组合 kit，`check:design-source` 绿；原生两端的选择器也收进宿主适配器）| P0 | check:design-source（出现即 FAIL） |
| 18 | Loading/Empty/Error 统一 | DONE | Vue 有 Skeleton/EmptyState/StatusBanner；Compose/iOS 的 WorkspaceFrame 不渲染任何状态，Flutter 降级（2026-09-13：四端抽出 WorkspacePane 原语，两个工作区共用；空态补说明与下一步，失败统一 danger + 带文字重试）| P0 | 四端状态截图矩阵 + 单测 |
| 19 | Controlled/Uncontrolled 清晰 | PARTIAL | Composer/Select/Dialog 混用 `open`/内部 visible（Composer activePanel 已受控）；无文档 | P1 | 每个 overlay 组件文档标 Controlled/Uncontrolled + 测试 |
| 20 | IME Composition PASS | DONE | Vue / Flutter 各自在组件里读组字标志，Compose / iOS 有契约函数但发送路径不走它（2026-09-13：`spec/form-keyboard-contract.json` 18 条向量四端同跑，组字守卫前移到第一位，composer 改为问契约，新增 `check:form-keyboard` 门禁）| P0 | 四端各一条 composition 测试 |
| 21 | Accessibility Critical=0 | DONE | 无 axe/自动化；本轮修：Checkbox 双触发、Stepper/Slider 焦点、Compose/Flutter 开关角色、Flutter 来电按钮语义（2026-09-13：axe 扫 191 个面 Critical 0）| P0 | Playwright + axe（website 嵌入帧）；原生 Semantics 测试 |
| 22 | Accessibility Serious=0 | DONE | 触控目标 <48 普遍；焦点环两套且半透明 outline 1.7:1（2026-09-13：axe Serious 0；触控目标改为「粗指针上命中区 ≥44×44」并逐个探测，39 个手机预览 0 失败）| P0 | 同上 + 命中区断言 |
| 23 | Visual Regression PASS | DONE | 记录已过期：四端实有 61 张基线（vue 54 / flutter 2 / ios 2 / compose 3）。真缺口是流程与平台耦合（2026-09-13：docs/release/visual-baselines.md 写明 EXPECTED/REGRESSION 判据；基线改按平台分目录；清单双向校验并入 check-visual-regression）| P0 | 基线 + EXPECTED/REGRESSION 人工可辨流程 |
| 24 | Responsive PASS | PARTIAL | ResponsiveLayout 向量四端；AppLayout 族本轮补 application-layout-vectors（14 例四端）；窄宽 320–1440 无系统性测试 | P1 | 10 档宽度 × 核心组件截图 + overflow 断言 |
| 25 | Dark Mode PASS | PARTIAL | token 驱动；但暗色下开关轨道曾不可见（已修）、原生暗色分组卡叠 primary glow（未修） | P1 | 六主题 × 暗色截图 |
| 26 | Token Audit PASS | PARTIAL | check-hardcoded-visuals 棘轮：vue 1391 / flutter 343 / ios 438 / compose 832 个等于 token 值的字面量；无字重刻度、无阴影原生表、无 fontWeight/onPrimary/scrim token | P1 | docs/release/token-audit.md + 棘轮只降 |
| 27 | Performance PASS | DONE | 无预算文档、无 1000 会话/5000 消息压测证据（2026-09-13：docs/release/performance.md + `website/tests/performance-budget.spec.ts` 真挂载测量，查出会话列表在宿主没约束高度时不虚拟化、消息列表阈值 25000 实际不窗口两件事）| P0 | docs/release/performance.md + 基准脚本 |
| 28 | Virtualization PASS | PARTIAL | MessageList/ConversationList 有虚拟化与锚定；原生 lazy 容器未验 | P1 | 滚动稳定性测试 |
| 29 | Browser Compatibility PASS | NONE | 无 docs/release/browser-compatibility.md | P1 | 四浏览器矩阵 |
| 30 | Dependency Audit PASS | FAIL | Vue 同时依赖 @vicons/ionicons5 与 @lucide/vue 两套图标；naive-ui 作为 peer 在 93 个组件文件里直接使用 | P1 | docs/release/dependency-audit.md + bundle size |
| 31 | Tree Shaking PASS | PARTIAL | package.json sideEffects ["*.css","*.vue"] 合理；未验证单组件 bundle | P1 | 打包一个组件的 size 证据 |
| 32 | Security Boundary PASS | DONE | RichText/Link/HTML 的 sanitization 归属未写明（2026-09-13：docs/release/security-boundary.md + `spec/security-boundary.json` 18 条 URL 向量四端同跑 + `check:security-boundary` 门禁盯住 v-html 白名单与自跳转组件）| P0 | docs/release/security-boundary（谁负责 sanitize）+ 测试 |
| 33 | Production Stub = 0 | NONE | 未跑 stub 扫描 | P1 | check:stubs |
| 34 | Flaky Core Tests = 0 | PARTIAL | 2026-09-13 实测到一条并已修：`accessibility-axe` 在负载 22–27 时 148 个预览有 55 个超时，且把"预览没渲染"记成 `critical` 级 a11y 违规。现在探测改为 3 次尝试 × 45s 退避，两类失败分开断言（先报 harness 再报 a11y），报告里 `harness` 与 axe 计数分离，并加了一条"扫描数必须等于目录数"的断言防止未来 `continue` 悄悄缩小扫描面。修后在同等负载（24–25）下 4/4 过、`unrendered` 0，其中 desktop 有 **2 个预览确实用到了重试**——旧的单次 20s 探测在这种负载下就会把它们报成两条 critical 违规。第二条同日实测到并已修：`visual-regression.spec.ts` 的 "timeline keeps one edge gutter" 在 load 24 时读到 10px（600–899 档）而期望 16px——gutter 由 `@media (min-width:)` 按视口驱动，而该测试是全仓**唯一**一处"页面加载后改视口再立刻测量"的调用点，且它 poll 的是内容宽度与列表宽度自洽（旧布局下同样成立），从未等待新视口真正生效。改为先 `expect.poll(window.innerWidth).toBe(width)` 再测量，断言本身一字未动；反向验证把条件改成 `width + 1` 后如期失败，修后在 load 42（高于失败时的 24）连跑 5 次全过 | P1 | 其余套件的 flaky 审计仍缺 |
| 35 | Browser Tests PASS | PASS | readiness 145 项；本轮 conversation-presentation 66/66 | — | 保持 |
| 36 | Native Contract Tests PASS | NONE | 见 15 | P0 | — |
| 37 | Source Fingerprint PASS | PASS | readiness | — | 最终候选重跑 |
| 38 | Public API Audit PASS | PASS | docs/release/public-api-2.0.md（2026-09-13 复核：149 进 149 出，P0-3 之后新增的公开面单列一节）；`check-public-api.mjs --strict` + `check-public-exports` + `check-package-exports` 全绿 | — | api-check 保持 strict |
| 39 | Migration Guide 完成 | PASS | docs/release/migration/2.0-rc-to-2.0.md：1/1b 删除合并、2 内部化、3 行为变更、4b–4i 对应 P0-5…P0-12（含断点 1023→899、`WorkspacePaneState.description`、Flutter 窗格类型更名、composer 送出判定改问契约、`safeExternalUrl`、基线分平台目录） | — | 新 breaking 必须同步加节 |
| 40 | release:check PASS | PARTIAL | 2026-09-13 端到端实跑五轮，最后一轮 **49 PASS / 1 FAIL**（50 条记录 = 43 具名关口 + 7 个 `git diff --check`），`candidate-integrity` PASS。唯一的红是 `web-live-sdk`：要求 `FLARE_ALLOW_LIVE_SEND=1` 与一套在跑的服务端，**没有联调环境的机器上整链不可能全绿**。过程中修掉 3 条 P0 遗留的陈旧产物、17 条样式归属违规、2 条 flaky（详见最终报告 §2）。readiness 仍判 NOT READY，原因只剩 13 条 manual evidence | P0 | 在有服务端的环境里整链重跑并全绿 |
| 41 | Final Report = READY | PARTIAL | docs/release/2.0-final-report.md 已出，结论是 **NOT READY → rc.2**（三类阻塞：真机/模拟器证据、CI Linux 基线、share/nativeBack 与媒体 URL 收口）；`tooling/check-release-report.mjs` 守住「必须表态、必须点名每个关口与每条未销证据、必须链到每篇分项文档」 | P0 | 结论转 READY 才算 PASS |

## 3. P0 清单（必须修，按执行顺序）

| # | 缺口 | 根因 | 动作 | 证据要求 |
|---|---|---|---|---|
| P0-1 | 两套消息体（D1）与 MessageActionSheet 名实不符（D8） | 时间线先于标准件演进，标准件为契约而造却未接回时间线 | 一类型一份（views 为实现，公开名 Flare*Message）；九宫格并入 ComposerActionPanel；MessageContextMenuSheet 登记为 MessageActionSheet；原生三端补长按面板 | catalog sourcePaths 指向的 symbol 与 contract.platforms.symbol 一致（新门禁）；四端 reactionStrip/quickActions/emojiExpanded 截图 |
| P0-2 | 平台契约缺失（DoD 11–15） | 没有 Layer 5；平台判断散落 | PlatformProvider + PlatformCapabilities（hover/contextMenu/bottomSheet/keyboardShortcut/nativeBack/safeArea/nativeFilePicker/nativeImagePicker/nativeShare/pointerType）+ PlatformAdapter 接口 + Native Error Model；Web/Tauri/iOS/Android/Flutter 各一实现；contract tests 覆盖 success/cancel/unsupported/denied/timeout/error | 四端契约测试 + platform-fallback-policy.md + native-capability-matrix.md |
| P0-3 | Public/Internal 边界（DoD 7、2、8） | index.ts 导出未受 catalog 约束；auth/diagnostics 混在 kit | 29 个未登记导出按 audit 表处理；auth/diagnostics/workbench shell 迁 examples/shared；catalog 加 maturity；check:public-exports 改为「导出 ⊆ catalog」 | 门禁 + maturity 表 |
| P0-4a | Dead / Duplicate 第一波（登记表 + 两条门禁；D3 TimeStamp→DatePill、D4 MessageList 用 ScrollToLatest、D5 PrimaryButton→Button、D10 7 个 Workspace 别名→WorkspaceFrame、D11 MasterDetail/ThreePane 内部化、D12 AppShell 删除、N1 DesktopWorkbench→DesktopAppShell（7 个键盘命令 Vue+Flutter）；Compose Screen/FormDialog 登记；零消费者符号清理） | DONE 2026-09-12（第二波 D6/D7/D9/D13/D14 待做，strict 门禁保持红） | `spec/duplication-register.json`；`node tooling/check-duplicate-components.mjs`；`node tooling/check-dead-components.mjs`；catalog 147；`./components` 154 = 147 + 7 pending；vitest 440 / check:sfc 191 / vue-tsc 0；flutter analyze 0 + 464 tests；compose 152；swift 189；Playwright visual + IA 43、conversation-presentation 56、全量 135；examples/flutter analyze 0、examples/compose compileDebugKotlin 0；docs/release/migration/2.0-rc-to-2.0.md；补丁 plans/audit-2026-09-12/patch-P04a-dead-duplicate.patch |
| P0-4b | Dead / Duplicate 第二波（pendingMerge 清零：D6 ChatHeader→ConversationHeader 四端、D7 StartConversationSheet 删除 + Dialog 随 bottomSheet 能力、D9 ConversationListPanel 与 iOS 泛型容器→Container、D13 MessagePreviewModal 归产品 + ComposerMediaPreview 登记、D14 EmojiStickerPicker 登记四端） | DONE 2026-09-13（api-check --strict 绿；duplicate/dead strict 只余 P0-5/P0-8 条目） | catalog 149；`./components` 149 = 149 + 0 pending；vitest 437 / check:sfc 186 / vue-tsc 0；flutter 464 + analyze 0；compose 152；swift 189；client-sdk web typecheck + vitest 48 + tauri 5 + ios app swift test 53；examples/flutter analyze 0、examples/vue vite build、examples/swiftui xcodebuild BUILD SUCCEEDED；docs/release/migration/2.0-rc-to-2.0.md §1b；补丁 plans/audit-2026-09-12/patch-P04-dead-duplicate.patch（两波累计）+ patch-P04-client-sdk.patch |
| P0-5 | Composer SPLIT（编排器 + 部件 + 组合式：`useVoiceRecorder` / `provideFlareVoiceRecorder` 适配器、`useFileDrop`、`useMarkdownShortcuts`；ComposerReplyStrip（+tone / cancelLabel）、ComposerSendButton、MentionPicker、ComposerActionPanel（支持组件图标）入 Composer；ComposerUploadStrip / ComposerFormatStrip / ComposerMoreSurface / ComposerVoicePanel 私有部件；messages.css 中遗留的回复条 / 渐变发送键 / 九宫格全局样式删除；D16 关闭；Flutter ComposerInlineTextField 登记为宿主部件） | DONE 2026-09-13（公开 API 不变） | EnhancedComposer.vue 1360→878 行、composer-studio.css 1105→645；vitest 450（新增 recorder 6 / file drop 2 / upload strip 2 / reply strip 2 / action panel 1）；check:sfc 190；vue-tsc 0；check:dead-components --strict 绿；Playwright 见下行 |
| P0-6 | Examples 不复制设计系统（DoD 17）：shared/vue-reference 与 examples/vue 去 naive-ui（Toast 栈 / WorkbenchOverlay / AppIcon 产品件，kit-auth.css 改 kit 类名）；kit 表单可标注（FormField `useId` + `for`/`aria-describedby`、Input id/aria/autocomplete/inputmode/invalid/size、Select aria-describedby、Icon 稳定类名）；iOS / Android 宿主适配器（P0-2 遗留）：`IosPlatformAdapter`（PHPicker / UIDocumentPicker，UIKit 呈现保证「选中」「取消」各一次回调）、`AndroidPlatformAdapter`（ActivityResultRegistry 注册 OpenMultipleDocuments / PickMultipleVisualMedia）；两端 composer 入口由 capabilities 决定 | DONE 2026-09-13 | `node tooling/check-design-source.mjs`（2 个示例根 177 文件只组合 kit）；`node tooling/check-platform-contract.mjs`（4 平台 × 18 向量）；32 项门禁全绿；kit vitest 452 / check:sfc 190 / vue-tsc 0 / Playwright 137；flutter 464 + analyze 0；compose 152；swift 189；示例：web typecheck + 50、tauri typecheck + 5、flutter app analyze 0 + 81、android app 20 单测 + compileDebugKotlin、ios app `xcodebuild -destination "platform=iOS Simulator,name=iPhone 17"` BUILD SUCCEEDED + swift test 59；随手修的真缺陷：`FlareUiProvider` 不带 `locale` 时会把宿主已设的语言重置成存储默认值（reference app 英文界面出现 24 小时制时间，complete-app 基线因此失配）—— 改为继承上层 i18n 上下文，加 2 条回归测试并反向验证；`verify-no-client-shadow-views.mjs` 会扫 Xcode 派生数据把 kit 资源副本误报成缺陷 —— 跳过构建产物目录并反向验证；补丁 plans/audit-2026-09-12/patch-P06-design-source.patch、patch-P06-client-sdk.patch |
| P0-7 | 可达性自动化（DoD 21、22）：axe-core 接进 Playwright（149 个组件的嵌入预览 × 桌面/移动 + 参考应用四屏）；触控命中区探测（粗指针 44×44 地板，`accessibility.css` 一处落地，桌面密度各自收进 `@media (pointer: fine)`）；`check-accessibility.mjs` 新增两条源码规则（naive 图标必须 `aria-hidden` 或 `aria-label`；四端图标默认装饰化）；release-check 新增 `accessibility-axe` 关口，视觉关口用 `--grep-invert @a11y` 不重复跑 | DONE 2026-09-13 | axe：191 个面（桌面 148 + 移动 39 + 参考应用 4）Critical 0 / Serious 0，报告 website/test-results/axe-{desktop,mobile,reference-app}.json；触控：39 个手机预览 0 失败；32 项门禁全绿；kit vitest 452 / check:sfc 190 / vue-tsc 0 / Playwright 141（137 视觉 + 4 可达性）；flutter 467 + analyze 0（两张 golden 因 token 收深重建）；compose 152；swift 189（两张快照同因重建）；示例 web typecheck + 50、tauri typecheck + 5；补丁 plans/audit-2026-09-12/patch-P07-accessibility.patch |
| P0-8 | 四端 Loading/Empty/Error（DoD 18）+ 重复登记表清零：抽出共享 `WorkspacePane` 原语（Vue `WorkspacePane.vue` / Compose `WorkspacePane` / iOS `WorkspacePaneView` / Flutter `FlareWorkspacePaneView`），`WorkspaceFrame` 与 `ConversationWorkspace` 两端都用它——此前 Compose / iOS 的 frame 连 state 参数都没有，Flutter 只有无文案的转圈和一枚无标签刷新图标；空态补 `description` 与「下一步」动作（新事件 `emptyAction`），失败一律 danger StatusBanner + 带文字重试；frame 的兜底文案独立成 `workspaceFrame.*`（它同时承载通讯录 / 设置 / 搜索 / 媒体，不能说「会话」）；D15 / D17 / N2 一并关闭：两套断点合成一处（`useViewport` 的 600/900 与 `useFlareAdaptive` 的 599/1023 之间，900–1023 曾经同时是「桌面 CSS + iPad 组件行为」），SceneList 改用 kit 部件（FlareButton + 设置分组卡）并把自己的第四套状态方言换成 WorkspacePane | DONE 2026-09-13 | `check:duplicate-components --strict` **20/20 全部关闭**（首次全绿）；`check:dead-components --strict` 绿；spec validate 绿；32 项门禁全绿；kit vitest 471（新增 WorkspacePane 5 / WorkspaceFrame 6 / SceneList 5 / breakpoints 3）；check:sfc 191；vue-tsc 0；Playwright 141；flutter 474 + analyze 0（新增 workspace_state_matrix_test.dart 7 例，真渲染断言骨架屏播报 / 空态下一步 / 失败重试 / 横幅 / 窗格互不影响）；compose 157（新增 WorkspaceStateMatrixTest 5）；swift 195（新增 WorkspaceStateMatrixTests 6）；示例：web typecheck + 50、tauri typecheck + 5、flutter app analyze 0 + 81；补丁 plans/audit-2026-09-12/patch-P08-workspace-states.patch |
| P0-9 | IME 四端证明（DoD 20）：审计发现四端其实都已有 `resolveFormKeyboardIntent` 与一条组字测试，真正的缺口是三处——(1) 组字守卫排在 Tab 分支之后，组字中按 Tab 会把焦点从半个词上挪走（四端同病，已前移到第一句，规则变成「组字中每个键都归输入法」）；(2) composer 的发送路径各读各的组字标志（Vue `event.isComposing`、Flutter `composing.isValid`），契约形同虚设，现改为问契约，Vue 并补上部分 Android/Windows 键盘只报 `keyCode 229` 的旧信号；(3) Compose 的 `BasicTextField` 在超出 maxLength 时整条丢弃更新，输入法对着一个 state 里已不存在的值继续组字，中日韩输入在边界上静默丢字——改成截断（`composerTextWithinLimit`），Composer 与 Input 两处 | DONE 2026-09-13 | `spec/form-keyboard-contract.json` 18 条向量 × 4 端，新门禁 `node tooling/check-form-keyboard.mjs`（校验四端符号存在、每条向量都被测到、守卫排在 Tab 之前、composer 的发送判定确实取自契约的 submit 意图），四条规则各做过反向验证；33 项门禁全绿（新增 1 条）；kit vitest 490 / check:sfc 191 / vue-tsc 0 / Playwright 141；flutter 496 + analyze 0（新增 composer_ime_test.dart 3 例真按键：组字中 Enter 不发、提交后同一个键才发、组字中 Ctrl+Enter 也不发）；compose 160；swift 197；示例：web typecheck + 50、tauri 5、flutter app 81；补丁 plans/audit-2026-09-12/patch-P09-ime-composition.patch |
| P0-10 | 性能预算（DoD 27）：原门禁只 grep 源码里有没有 `LazyColumn` / `virtualItems` 这些词——词在不等于窗口生效。本轮加 `/resources/performance-regression` fixture 真挂 1000 会话 + 5000 消息数 DOM，查出两件事：(1) `.im-conv-list` 声明了 `overflow-y: auto` 却不约束自身高度，在 900px 容器里长到 72000px，虚拟窗口据此覆盖全部 1000 行，契约写的 O(visible) 悄悄不成立且无任何报错——加 `height: 100%` 自兜底（父高 auto 时解析回 auto，只在宿主真给了盒子时生效），渲染行数 1000→21、挂载 1414ms→1002ms；(2) `MessageList` 的 `VIRTUALIZATION_THRESHOLD = 25_000` 让契约的 `message10k` 场景实际渲染一万行。代码注释写明原因（固定行高估算 vs 富文本行高可变，翻页时滚动条会跳），**不改实现**，改契约：把守不住的「组件保证 O(visible)」换成真实保证「组件渲染宿主加载了多少就有多少，边界属于宿主分页窗口」，预算落到每行 DOM 成本与 `hostMaxLoadedMessages: 400` | DONE 2026-09-13 | docs/release/performance.md；`spec/performance-contract.json` 新增 `measuredBudgets`；`check-performance.mjs` 增校验（数值预算齐备、不窗口的组件必须写明原因），两条规则各反向验证；新 release-check 关口 `performance-budget`（`--grep @perf`），视觉关口改 `--grep-invert "@a11y|@perf"` 不重复跑；实测：会话 1000 条渲染 21 行 / 每行约 15 节点、消息 5000 条每行约 35 节点（171678 节点）、满载挂载 1153ms，报告 website/test-results/performance.json；门禁 33 项全绿；kit vitest 490 / check:sfc 191 / vue-tsc 0；Playwright 137 视觉 + 4 可达性 + 2 性能；示例 web typecheck + 50、tauri 5；补丁 plans/audit-2026-09-12/patch-P10-performance.patch |
| P0-11 | 安全边界（DoD 32）：审计结论是 kit 的现状其实是稳的——markdown-it 走 `html: false`、`javascript:` 链接被 markdown-it 的 validateLink 挡掉、原始 HTML 被转义、kit 几乎从不自己跳转（只有 LinkCardView 与 LocationView 两处 `<a>`，前者已有 http/https 判断、后者由经纬度拼出）。缺的是三样：没写明归属、没有回归测试、没有门禁——一个新的 `v-html` 或一个直接绑到消息内容的 `:href` 会在一个讲别的事情的 diff 里悄悄丢掉这条边界。本轮补：`safeExternalUrl` 四端落地（此前只有 LinkCardView 里的一段内联判断，宿主无从复用同一条规则）；18 条 URL 向量四端同跑；markdown 恶意输入回归测试；`spec/security-boundary.json` + `check:security-boundary` 门禁；docs/release/security-boundary.md 写明 SDK / kit / 宿主三层各管什么。四端对齐时向量表当场查出一处真分歧：把控制字符连空格一起剥会把 `just some text` 捏成合法主机名——Dart 的 `Uri` 会百分号编码后放行，JS 的 `URL` 会抛，四端行为不一致；改成只剥制表符与换行、并对裸空格一律拒绝，四端靠同一条规则对齐而不是靠谁的解析器更严 | DONE 2026-09-13 | `node tooling/check-security-boundary.mjs`（4 个 URL 闸 × 18 向量、3 处 v-html 白名单、2 个自跳转组件），四条规则各反向验证（越界的 v-html、闸被摘掉的 anchor、`html: true`、少验一条向量）；34 项门禁全绿（新增 1 条）；kit vitest 515 / check:sfc 191 / vue-tsc 0 / Playwright 143；flutter 500 + analyze 0；compose 164；swift 201；示例 web typecheck + 50、tauri 5、flutter app 81；补丁 plans/audit-2026-09-12/patch-P11-security-boundary.patch |
| P0-12 | 视觉基线（DoD 23）：审计先推翻了记录——四端其实已有 61 张像素基线（vue 54 / flutter 2 / ios 2 / compose 3），不是「0 png」。真缺口有三个：(1) `snapshotPathTemplate` 把平台段去掉了，一套 PNG 要在所有系统上匹配——而并行会话新写的 CI 视觉任务跑在 ubuntu 上，基线是 macOS 渲染的，凡是带字的用例第一次跑就会红；改成按平台分目录（现有 54 张归入 `darwin/`），Linux 那套要在 CI 环境里生成，本机 arm64 生成的不算数；(2) 没有写下 EXPECTED / REGRESSION 判据——重建基线太顺手，一条真实回归跑一次 `--update-snapshots` 就变成「通过」；本轮把判据表和本轮三次重建的实际判断（complete-app 判为 REGRESSION 去修代码、Flutter 与 iOS 判为 EXPECTED 重建）写进文档；(3) 清单只单向校验「声明的基线在不在」，没人拦「多出来一张没人声明的 PNG」。实现上没有另起炉灶：新规则并入既有的 `tests/visual/manifest.json` + `check-visual-regression.mjs`（我一度另写了一份 spec + 门禁，发现与既有清单重复后删掉） | DONE 2026-09-13 | docs/release/visual-baselines.md；`check-visual-regression` 现在双向校验 61 张基线、拦平台目录外的散落 PNG、要求配置里带 `{platform}`、要求流程文档存在，四条规则各反向验证；34 项门禁全绿；Playwright 143（137 视觉 + 4 可达性 + 2 性能）全部对着 `darwin/` 下的基线跑通；补丁 plans/audit-2026-09-12/patch-P12-visual-baselines.patch |
| P0-13 | API / 迁移 / 最终报告（DoD 38–41）：审计发现前三件其实已随各 P0 滚动写到位——public-api-2.0.md、migration/2.0-rc-to-2.0.md 都在，缺的是(1)它们停留在 P0-3 / P0-12 当时的数字，(2)没有最终报告，(3)没有任何门禁拦「报告与实际门禁清单脱节」。组件目录不另写 Markdown 副本：真源是生成的 `spec/component-catalog.json` 与站点的同名文件，再抄一份只会漂移。最终报告按证据写，因此结论是 NOT READY 而不是 READY——13 条 manual evidence 未销，写 READY 才是降低标准 | DONE 2026-09-13 | docs/release/2.0-final-report.md（结论 NOT READY → rc.2，三类阻塞逐条点名）；`tooling/check-release-report.mjs` 四条规则各反向验证（加假关口→红、删掉一条未销证据 id→红、移走一篇分项文档→红、恢复→绿），并在写作过程中真的抓出我自己漏掉的一处「9 条未落地」没点名；public-api-2.0.md / migration §5 / final-report 的门禁数同步到 35 + 41；35 项设计系统门禁全绿、doc-links 553 篇零死链；补丁 plans/audit-2026-09-12/patch-P13-release-docs.patch |

已在本轮以补丁落地的 P0（保留证据，纳入 rc.2）：Checkbox 双触发；Stepper/Slider 焦点不可见；Compose Switch/Checkbox/RadioGroup 与 Flutter Checkbox/RadioGroup 读屏语义；Flutter IncomingCall 按钮语义与英文文案；三原生 GroupCallView/CallControls 加人键（契约 eventPlatforms 四端）；AppLayout 族 900–1259px 导航压列表（四端宽度感知 + 共享向量 + Compose/iOS 断点统一）。

### 3.1 执行进度

| # | 子项 | 状态 | 证据 |
|---|---|---|---|
| P0-1a | 一类型一份：时间线 views → standalone Flare*Message adapter（Text/Voice 原有；System/Location/Card/LinkCard/Sticker/Emoji/File/Image/Video/Task 本轮） | DONE 2026-09-12 | `packages/vue-im-ui/src/components/messages/MessagesView/ContentView.test.ts` 13 例；vitest 423/423；check:sfc 204；vue-tsc 0；Playwright 145/145（complete-app 参考应用基线因会话行字重梯度与列表 scrollbar-gutter 过期，已核对后重建）；spec 新增 Vue 注册 props（FileMessage description/state/downloadLabel/embedded，ImageMessage maxWidth/maxHeight/badge/loading，VideoMessage maxWidth/maxHeight/loading/disabled，TaskMessage embedded）；hardcoded-visuals vue 1494→1364 已锁基线；截图 plans/audit-2026-09-12/sheets/p01-*、p01b-*；补丁 plans/audit-2026-09-12/patch-P01a-message-bodies.patch |
| P0-1b | MessageActionSheet 名实（九宫格并入 ComposerActionPanel；MessageContextMenuSheet 登记；原生三端长按面板；sourcePaths↔symbol 门禁） | DONE 2026-09-12 | 四端 MessageActionSheet 均为长按面板、九宫格只剩 ComposerActionPanel（见 component-duplication.md D8）；sourcePaths↔symbol 由 `tooling/build-component-catalog.mjs` 按符号定位 + `spec/validate.mjs` 回调/事件核对承担，无需新增门禁；vitest 424、flutter 441、compose 31 XML、swift 185、Playwright 145、spec validate OK；截图 plans/audit-2026-09-12/sheets/p01c-*；补丁 patch-P01b-message-action-sheet.patch |
| P0-2 | 平台契约层（PlatformCapabilities / PlatformAdapter / Native Error Model / 五端实现 / 契约测试 / fallback policy / capability matrix） | DONE 2026-09-12（kit 四端 + web/tauri/flutter 宿主；iOS/Android 宿主适配器 2026-09-13 随 P0-6 补齐；真机往返未覆盖） | spec/platform-contract.json；`node tooling/check-platform-contract.mjs`（4 平台 × 18 向量、sniffLint 0，release-check `native-contract`）；vitest 446 / flutter 464 / compose 152 / swift 189；docs/release/platform-fallback-policy.md、native-capability-matrix.md；补丁 patch-P02-platform-contract.patch、patch-P02-examples.patch |
| P0-3 | Public/Internal 边界（api-check；29 个未登记导出处置；产品代码迁出 kit） | DONE 2026-09-12（7 项 pendingMerge 留给 P0-4，strict 门禁保持红） | `node tooling/check-public-api.mjs --strict`；spec/public-api-exceptions.json；docs/release/public-api-2.0.md；catalog 159；vitest 439 / Playwright 145 / web+tauri typecheck & tests / swift 189；补丁 patch-P03-public-api.patch、patch-P03-examples.patch |

## 4. P1（低风险、明显降低维护成本，可在 rc 内做）

2026-09-13 全部执行完毕。审计先量后改，八条里有两条本来就已落地、一条只剩计划外的真缺陷，逐条结论：

| 原计划 | 实测 | 处置 |
|---|---|---|
| 统一焦点环工具类 | **不是整理，是 a11y 缺陷。** 焦点轮廓有三套取色。逐主题算对比度（7 主题 × 明暗 = 14 组）：`focus-ring` 1.47–2.97、`primary` 在 7 个暗色主题全是 1.80–2.66，都低于 WCAG 2.4.11 的 3:1；只有 `border-selected` 14 组全过（最低 3.30）。四端共 61 处取错。axe 不测焦点指示对比度，所以 P0-7 的 191 个面全绿也没暴露 | DONE：四端收敛到 `border-selected`；媒体层用 `white`、语义色表面用 `currentColor` 两个例外入白名单；`check-accessibility.mjs` 双侧新规则；`docs/accessibility-contract.md` 新增 Focus indicator 一节并附 14 组实测表 |
| Flutter 硬编码英文；Vue Stepper/Slider/FilterTabs 英文 aria-label | 属实。Flutter 通话 5 条 + dock 1 条 + 语音条 1 条，键在 `FlareStrings` 里早就有、组件没用；会话行 `'[Draft] '`/`'[@me] '` 是构造默认值，Compose 同样，只有 iOS 走了字符串表；Vue 三条硬编码；FilterTabs 是 prop，没问题 | DONE：全部走字符串表；Flutter/Compose 两表各补 `conversationRowDraft`/`conversationRowMention`；门禁加 requireText + 两表镜像校验 |
| 两套图标库收敛到 Lucide | kit 早已整体走 `shared/icon-glyphs`（ionicons 名 → Lucide 字形）。只剩 `EnhancedComposer` 一个文件直连 `@vicons/ionicons5`（3 个字形，shim 里都有），它一个人让整包多一个运行时依赖 | DONE：改走 shim，依赖移出 manifest，`check-package-boundaries.mjs` 两条新规则 |
| `--flare-component-brand-gradient` 删或真渐变 | 该 token 存在且等于 `var(--flare-color-primary)`——纯色，名字在说谎，且与 `--flare-component-brand-primary` 重复 | DONE：删除，12 个文件改指后者，契约摘要中英文同改。顺带补了更有用的一条：引用不存在的 `var(--flare-*)` 会静默失效，`check-legacy-im-tokens.mjs` 新增未定义 token 检查 |
| 断点单源 | 常量已合一（P0-8 的 N2），但 `application.ts` 仍写死 600/900/1500、`desktop-workbench.ts` 写死 900/1500 | DONE：改读常量（新增 `FLARE_BREAKPOINT_WIDE_DESKTOP_MIN`）；`layout.test.ts` 把 TS 常量直接对着 `tokens/tokens.json` 断言 |
| 四端几何统一 | 只有两处真裂开：Vue Button / Input 用 `radius-md`(8)，其余三端都是 `radiusLg`(10)。IconButton 32/40/48 四端本就一致（同读 `controlHeight*` token）；GroupList 四端都没写死行高，计划里的 64→60 无处可改 | DONE：Vue 对齐 10。像素基线看不见这种差异（`maxDiffPixelRatio: 0.002`，两像素圆角远低于阈值），因此在 `check-component-contracts.mjs` 加四端 radius token 同名校验 |
| 文档/预览噪音四项 | 三项早已落地（embed frame `layout: false` + `embedded` 去壳；图片预览 demo 默认关闭带触发按钮；`previewState` 这个名字已不存在，透传的是 `initialViewport`）。第四项是真 bug：`ImageGridDemo` 先 `.replace(/#/g, "%23")` 再 `encodeURIComponent`，二次编码成 `%2523`，渐变与 `url(#g)` 全部失效 | DONE：删掉预编码，round-trip 验证 |
| MessageStatus 契约对齐 / 原生阴影表 / 暗色 primary glow / 字重 500/700 | 已完成：契约是 `status/lifecycle/variant` 四端齐备并由 `spec/validate.mjs` 守；`tokens/build.mjs` 已生成 `FlareShadows` 明暗两套；暗色阴影已是纯黑无紫色辉光；字重阶梯此前已四端落地 | 无需改动 |

遗留：`ConversationActionSheet` / `MemberRoleSheet` 一类菜单行在 Flutter/Compose 上用 `bgHover` 作为焦点底色（面积型指示，也不到 3:1）。没有跟着本轮改，因为把菜单高亮换成紫环会与三端的原生菜单观感冲突，属于需要设计决定的改动，不是 P1 的"低风险"。

## 5. P2（原定 rc 阶段不做；2026-09-13 按要求逐条审计后，做了三条、拒了三条）

| 项 | 实测 | 处置 |
|---|---|---|
| SSR 支持（"当前 16 个组件在渲染期访问 window/document，记录为 client-only 即可"） | **原始记录是错的。** 用 `@vue/compiler-sfc` + Babel 解析每个模块（而不是 grep）后：25 个模块提到 `window`/`document`，**渲染期访问是 0**——全部在 `onMounted` / 事件处理器里，或者有 `typeof window === "undefined"` 守卫。`MessageList` 那处是局部变量 `const window = virtualWindow.value` 的同名遮蔽，第一版扫描的误报 | DONE：不是"记录为 client-only"，而是把测量本身固化成门禁 `tooling/check-ssr-safety.mjs`（零违规棘轮，区分 setup 作用域 / `computed()` / 已延后的回调，并处理同名遮蔽）。三个反向验证：setup 作用域访问→红、`computed()` 里访问→红、局部同名变量→不误报 |
| 装饰性 hover 位移清理 | 15 条 `:hover` 规则带 transform，其中 6 条是真位移（`translateY(-1px/-2px)`）。位移会搬动 border box：光标停在元素将要让出的那 1–2px 上，hover 掉→元素落回→hover 再起。另外三端（Flutter/Compose/iOS）的 hover 全是换色，没有位移 | DONE：6 条位移全部换成不搬动命中区的反馈（背景 / 边框 / 阴影）；原地放大的 `scale(>1)` 4 条保留（命中区只会变大）。新增门禁规则，反向验证包含"藏在 scale 后面的位移" |
| UnknownMessage 等宽字体角色 | 等宽字体栈在 6 处声明里有 **3 种拼法**，且没有 token，宿主改不了 | DONE：新增 `--flare-component-font-family-mono`，6 处统一 |
| CommentThread / MomentCard 派生色替换 | 全库 `color-mix` 有 **230 处、48 个文件**；hover 背景里 25 处用 `--flare-color-bg-hover`、20 处派生。这两个组件不是异类，很多派生是合理的（气泡内按 `currentColor` 取色、危险操作按 `error` 取色） | **不做。** 从 230 处里挑 2 处改属于任意改动；真正要回答的是"什么时候允许派生"——那是设计决定，不是机械替换 |
| 命名洁癖（ChatWorkspace→ChatView 等） | `ChatWorkspace` 的契约摘要是"Context → Header → Timeline → Composer 的组合面"，它确实是工作区不是叶子视图；改成 `ChatView` 反而更不准。它和 `WorkspaceFrame` / `ConversationWorkspace` 是一族命名 | **不做。** 原则 13：不允许无意义 breaking change。名实不符才改（P0-1b 的 MessageActionSheet 就是真不符，已改） |
| RTL 全面重构 | 162 处物理方向声明散在 60 个文件（85 处已是逻辑属性），且全库没有任何 `[dir="rtl"]` 处理；四端还要各自处理气泡尾巴、图标镜像、滑动方向 | **不做。** 体量与风险都远超 rc，需要单独立项 |

## 6. 版本判定

- 当前：2.0.0-rc.1，保持。
- 本轮修复合入 + P0-1…P0-13 全部落地并有证据后仍需真机（VoiceOver/TalkBack、硬件性能）：2.0.0-rc.2。
- DoD 全 PASS（真机 MANUAL 项完成）：2.0.0。

---
title: DeviceSessions
---

# DeviceSessions

Host-controlled scene composition. Data, capabilities, permissions and async operations remain in SDK adapters.

<div class="flare-demo flare-demo--stack"><ScenePanelsDemo mode="DeviceSessions" /></div>

<ComponentApi name="DeviceSessions" />

See the [integration and state contract](/components/device-sessions). Native entry points are FlareDeviceSessions (Flutter), DeviceSessionsView (SwiftUI), and DeviceSessions (Compose). Tests and local demos do not replace real-device or backend acceptance.
